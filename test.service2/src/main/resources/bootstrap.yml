spring.application.name: service2
spring:
  profiles:
    active:
    - base
    - server
  cloud:
    config:
     uri: http://config:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry1/eureka/,http://registry2/eureka/