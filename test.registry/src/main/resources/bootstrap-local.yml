spring.application.name: registry
spring:
  profiles:
    active:
    - base
    - local
  cloud:
    config:
     uri: http://localhost:8888
     
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false